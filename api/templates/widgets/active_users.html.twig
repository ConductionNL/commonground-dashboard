<div class="col-md-4">
    <div class="card card-primary bg-primary-gradient">
        <div class="card-body">
            <h4 class="mt-3 b-b1 pb-2 mb-4 fw-bold">Total requests</h4>
            <h1 class="mb-4 fw-bold">{{ requests['hydra:member']|length }}</h1>
            <h4 class="mt-3 b-b1 pb-2 mb-5 fw-bold">Werkvooraad</h4>
            <h1 class="mb-4 fw-bold">{{ requests['hydra:member']|length }}</h1>
            
            <h4 class="mt-5 pb-3 mb-0 fw-bold">Type of active requests</h4>
            <ul class="list-unstyled">
                <li class="d-flex justify-content-between pb-1 pt-1"><small>Verhuizen</small>
                    <span>7</span></li>
                <li class="d-flex justify-content-between pb-1 pt-1"><small>Huwelijk / Partnerschap</small>
                    <span>10</span></li>
            </ul>
        </div>
    </div>
</div>
