<div class="col-md-12">
    <div class="card full-height">
        <table class="table table-hover">
            <thead>
            <tr>
                <th scope="col">Alarm</th>
                <th scope="col">Status</th>
                <th scope="col">Organization</th>
                <th scope="col">Submitter</th>
                <th scope="col"></th>
            </tr>
            </thead>
            <tbody>
			{% for index, agenda in agendas._embedded.item %}
            <tr>
                <td>{{ agenda.alarm}}</td>
                <td>{{ agenda.status }}</td>
                <td>{{ agenda.target_organization }}</td>
                <td>{{ request.submitter }}</td>
                <td><a href="{{ path('app_request_edit',{'id':request.id}) }}">Edit</a></td>
            </tr>
			{% endfor %}
            </tbody>
        </table>
    </div>
</div>
