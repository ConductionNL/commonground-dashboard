<div class="col-md-12">
    <div class="card full-height">
        <table class="table table-hover">
            <thead>
            <tr>
                <th scope="col">Type</th>
                <th scope="col">Status</th>
                <th scope="col">Organization</th>
                <th scope="col">Submitter</th>
                <th scope="col"></th>
            </tr>
            </thead>
            <tbody>
			{% for index, archive in archives._embedded.item %}
            <tr>
                <td>{{ archive.id }}</td>
                <td>{{ archive.nomination }}</td>
                <td>{{ archive.status }}</td>
                <td><a href="{{ path('app_verzoekreg_edit',{'id':request.id}) }}">Edit</a></td>
            </tr>
			{% endfor %}
            </tbody>
        </table>
    </div>
</div>
