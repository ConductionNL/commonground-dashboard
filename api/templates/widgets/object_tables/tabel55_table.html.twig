<div class="col-md-12">
    <div class="card full-height">
        <table class="table table-hover">
            <thead>
            <tr>
                <th scope="col">Afnemersaanduidng</th>
                <th scope="col">Omschrijving</th>
                <th scope="col"></th>
            </tr>
            </thead>
            <tbody>
            {% for index, tabel55 in tabel55s %}
                <tr>
                    <td>{{ tabel55.afnemersaanduiding }}</td>
                    <td>{{ tabel55.omschrijving }}</td>
                    <td><a href="{{ path('app_landelijk_edit',{'id':tabel55.id}) }}">Edit</a></td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
