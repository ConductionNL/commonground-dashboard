<div class="modal fade" style="text-align: center" id="{{ id }}{% if item.id is defined %}{{ item.id }}{% endif %}" tabindex="-1"
     role="dialog"
     aria-labelledby="{{ id }}Title" aria-hidden="true">
    <div class="modal-dialog model-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="{{ id }}Title"><b>{% if item.id is defined %}{{ 'edit user'|trans|capitalize }}{{ item.id }}{%  else %}{{ 'add user'|trans|capitalize }}{% endif %}</b></h5>
                <button type="button" class="close" data-dismiss="modal"
                        aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <form action="{{ path(route, {id: resource.id}) }}" method="post">
                <div class="form-group">
                    <label for="largeInput">{{ 'user'|trans|capitalize }}</label>
                    <select name="user[user]" id="user[user]" class="form-control form-control-lg" required>
                        {% for user in newUsers %}
                            <option value="{{ user['@id']}}" {% if item.user is defined and item.user == user['@id']  %}selected{% endif %}>{{ user.username }}</option>
                        {% endfor %}
                    </select>
                </div><!-- /.form-group -->

                {% if item['id'] is defined %}
                    <input type="hidden" id="user[id]" name ="user[id]" value="{{ item['id'] }}">
                {% endif %}
                {% if item['@id'] is defined %}
                    <input type="hidden" id="user[@id]" name ="user[@id]" value="{{ item['@id'] }}">
                {% endif %}
                <button type="submit" class="btn btn-success m-3  mx-auto">
                    {% if item.id is defined %}
                        {{ 'modify'|trans|capitalize }}
                    {% else %}
                        {{ 'add'|trans|capitalize }}
                    {% endif %}
                </button>
            </form>
        </div>
    </div>
</div>
