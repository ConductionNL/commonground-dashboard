
<div class="form-group">
    <div>
        <label for="{{ property.id }}">{{ property.title }}</label>
    </div>
    <textarea class="form-control" id="{{ property.id }}"
              name="newProp">{% if value is defined and value is not empty %}{{ value }}{% endif %}</textarea>
    <input type="hidden" value="{{ property.name }}" name="newPropName">
</div>

